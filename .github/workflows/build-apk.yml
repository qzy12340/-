name: 构建多肽 APK

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true
    steps:
      - uses: actions/checkout@v4

      - name: 查看目录结构
        run: ls -la

      - name: 设置 Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: 设置 Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: 安装依赖并同步
        run: |
          cd duotai
          npm install
          npx cap sync android

      - uses: android-actions/setup-android@v3

      - name: 构建 APK
        run: |
          cd duotai/android
          chmod +x gradlew
          ./gradlew assembleDebug

      - uses: actions/upload-artifact@v4
        with:
          name: 多肽-调试版
          path: duotai/android/app/build/outputs/apk/debug/*.apk
